questions:
- variable: size
  default: "4Gi"
  description: "Size of the storage drive."
  type: string
  label: "Size: "
  required: true
- variable: existingVolumeName
  description: "Bind to an existing volume by name (must be unbound); if empty will create dynamic PV."
  label: "Existing Volume Name: "

